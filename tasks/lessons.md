# Lessons

## UI Refactors

- When redesigning the frontend, prefer the repo's actual shadcn dashboard primitives and layout patterns over custom one-off shells.
- Do not use skeleton placeholders for primary page loading states in Deployik. Use a spinner-based loading state instead.
- When the product has shallow global navigation, prefer a top navbar plus command palette over a persistent sidebar.

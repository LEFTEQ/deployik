# UI Layout - Patterns & Rules
> Load when touching sidebar, layouts (workspace vs project), ProjectPicker, CommandPalette, or the release dialog.
> Last validated: 2026-04-16

## Layout Tree

```
AppLayout              SidebarProvider wrapper (protected shell) — outermost
 ├─ WorkspaceLayout    Sidebar (workspace nav) + breadcrumb header + <Outlet>
 │   └─ Projects page
 ├─ NewProject         full-width, reuses AppLayout only
 └─ ProjectLayout      Sidebar (project nav, with Settings collapsible) + breadcrumb header + <Outlet>
      └─ Project pages (Overview, Deployments, Analytics, Integration, Settings/*)
```

All three layouts share `AppSidebar` (`components/layout/AppSidebar.tsx`) with a `context: 'workspace' | 'project'` prop. Project context also needs `projectId`.

## `AppSidebar` Responsibilities

| Slot | Content |
|------|---------|
| Header (`SidebarHeader`) | Workspace context → Deployik logo + "Dashboard" subtitle. Project context → `<ProjectPicker />` (command-based switcher) |
| Content group 1 | Nav items: workspace (Projects only) OR project (Overview, Deployments, Analytics, Integration, Settings with subitems Build/Domains/Environments/Protection) |
| Content group 2 | Quick actions: "All Projects" (project context), "New Project" |
| Footer (`SidebarFooter`) | User avatar menu → workspace radio group + Log Out |

The Settings item uses `<Collapsible defaultOpen>` with `SidebarMenuSub` children so the 4 sub-routes are always visible when any `/settings/*` path is active.

## Command Patterns

| Component | Trigger | Behavior |
|-----------|---------|----------|
| `ProjectPicker` (sidebar header, project context) | Click current project pill | Command list of all projects; Enter navigates to `/projects/{id}` |
| `CommandPalette` (global Cmd/Ctrl+K) | Keyboard shortcut | Actions + workspaces + project search; single entry point to anywhere |

Both use `cmdk` via the shadcn `command` primitive.

## Release Dialog (post-redesign, commit 2c624ae)

**Shape:** `ReleasePanelContent` (`components/projects/release-panel.tsx`) is a **pure content component**, not a standalone panel. It is rendered inside:
- `ProjectOverview.tsx` — inside a `<Dialog>` triggered by the "Release to production" action.
- `ProjectDeployments.tsx` — same dialog pattern for triggering a production deployment from the list view.

Props:
```ts
{
  project: Project;
  domains: Domain[] | undefined;
  createTag: boolean;
  onCreateTagChange: (value: boolean) => void;
  releaseTagName: string;
  onReleaseTagChange: (value: string) => void;
}
```

The parent page owns the mutation (`api.triggerDeployment(projectId, { environment: 'production', create_tag, tag_name })`) and handles close-on-success.

**Default tag name** is built by `buildReleaseTagName(new Date())` from `deployment-helpers.ts` → `release-YYYYMMDD-HHmm`.

## shadcn Sidebar Primitives (`components/ui/sidebar.tsx`)

Bundled from shadcn; used throughout:
- `SidebarProvider`, `Sidebar`, `SidebarInset`, `SidebarTrigger`, `SidebarRail`
- `SidebarHeader`, `SidebarContent`, `SidebarFooter`
- `SidebarGroup`, `SidebarGroupLabel`
- `SidebarMenu`, `SidebarMenuItem`, `SidebarMenuButton`
- `SidebarMenuSub`, `SidebarMenuSubItem`, `SidebarMenuSubButton`

Variant/collapsible: `<Sidebar variant="sidebar" collapsible="icon">` — collapses to icon rail on narrow screens.

## Do / Don't

| Do | Don't | Why |
|----|-------|-----|
| Use `SidebarMenuButton asChild + <Link>` for nav items | Nest `<button>` inside `<Link>` | Accessibility + styling breaks otherwise |
| Drive `isActive` from `useRouterState().location.pathname` via a `matchPath(pathname)` closure | Compare location.pathname inline in JSX | Sub-route patterns like `/settings/*` are cleaner as predicates |
| Keep project context in the sidebar via `context + projectId` props | Thread routes through global state | Component stays pure, easy to reuse |
| Reuse `components/ui/breadcrumb.tsx` in `ProjectLayout` header | Build a custom breadcrumb | Consistent spacing/typography |
| For a new dialog, pair with existing `Dialog` primitive + a Content component in `components/projects/` | Inline 200-line dialogs inside pages | Matches the `ReleasePanelContent` pattern |

## File Map (quick recall)

| File | Purpose |
|------|---------|
| `layout/AppLayout.tsx` | SidebarProvider wrapper — outermost protected shell |
| `layout/WorkspaceLayout.tsx` | Workspace-scoped layout (Projects list) |
| `layout/ProjectLayout.tsx` | Project-scoped layout with breadcrumb |
| `layout/AppSidebar.tsx` | The one sidebar; switches content by `context` prop |
| `layout/ProjectPicker.tsx` | Command-based project switcher in sidebar header |
| `layout/CommandPalette.tsx` | Global Cmd/Ctrl+K palette |
| `layout/TopBar.tsx` | Legacy top bar — KEPT FILE, NOT USED in main layout. Remove-on-next-pass candidate. |
| `projects/release-panel.tsx` | `ReleasePanelContent` — dialog content for production release |
| `projects/build-settings.tsx` | `BuildSettingsFields` — shared between NewProject & ProjectSettings |
| `projects/variable-store.tsx` | Vercel-style env/secret CRUD with .env import |
| `projects/dns-setup-guide.tsx` | Collapsible DNS helper with VPS IP from `/api/platform` |
| `projects/overview-stat-card.tsx` | Environment stat card on the Overview page |

## Gotchas

- **`TopBar.tsx` still exists** (from pre-redesign) but is NOT wired into any layout. Do not import it; if you do, it will double-render nav. Slated for removal.
- **`Projects` page lives inside `WorkspaceLayout`** BUT `NewProject` uses `AppLayout` directly (no sidebar-nav items exist for it). The project-nav sidebar is only for `/projects/$id/*`.
- **`ProjectPicker` currentProjectName** falls back to `"..."` while the project query is loading — that's intentional; don't show a spinner (looks worse).
- **Dark theme only** — `index.css` / `styles.css` set zinc palette. Light theme is not implemented; any component assuming `dark:` prefix toggling will misbehave.
- **SidebarProvider cookies** — shadcn's sidebar persists collapsed state in a cookie by default. If that conflicts with SSR/auth cookies, check cookie names.

## Deep Reference Trail
For the full release dialog state diagram, CommandPalette action registry, ProjectPicker keyboard wiring, and responsive/mobile breakpoints → see `L2.md` (not yet created — add when needed).

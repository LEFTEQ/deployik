{
  "name": "@lovinka/deployik-mcp",
  "version": "0.5.0",
  "description": "MCP server for Deployik — drive projects, deployments, secrets, and domains from any MCP-aware AI.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "deployik-mcp": "dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "prebuild": "node scripts/copy-recipes.mjs",
    "build": "tsc -p tsconfig.json",
    "dev": "node scripts/copy-recipes.mjs && tsc -p tsconfig.json --watch",
    "start": "node dist/index.js",
    "inspect": "node scripts/copy-recipes.mjs && tsc -p tsconfig.json && npx @modelcontextprotocol/inspector node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node scripts/copy-recipes.mjs && bun test",
    "prepublishOnly": "npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "deployik",
    "lovinka",
    "ai"
  ],
  "author": "Lovinka",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lefteq/lovinka-deployik.git",
    "directory": "mcp"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.18.0",
    "ws": "^8.20.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "@types/node": "^22.10.0",
    "@types/ws": "^8.18.1",
    "typescript": "^5.8.0"
  }
}
